{% if post.terms('tags') %}
<div class="sidebar-widget">
	<h3>Tags</h3>
		<div class="task-tags">
        {% for term in post.terms('tags') %}
			<a href="#"><span>{{term.name}}</span></a>
        {% endfor %}
		</div>
</div>
{% endif %}